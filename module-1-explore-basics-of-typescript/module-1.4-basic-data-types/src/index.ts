let test: string = 'Code is testing...';
console.log(test);

// data types

// number
let id: number = 123;
console.log(id);

// boolean
const isBoolean: boolean = true;

// undefined
let x: undefined = undefined;

// null
let y: null = null;

// any type
let any;

// Array

const friends: string[] = ['berlin', 'tokyo'];
const rollNumber: number[] = [1, 2, 3, 4, 5, 6, 7];

rollNumber.push(45);

const myName: string = 'me';

// tuple
const tupleArray: [number, number] = [1, 2];